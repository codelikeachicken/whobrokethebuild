const chalk = require('chalk')
const watcher = require('watch')
const search = require('lunr')
const fs = require('fs')

module.exports = function(dir, find_what, options) {
    console.log(chalk.green(chalk.bold('module.exports.find.js')))
};